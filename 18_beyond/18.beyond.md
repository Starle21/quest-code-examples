well behaved systems:

0
having control of our code, knowing that we are getting right results, bug free code,
handling change, extending program easily
1
data, code
thread of execution, control flow, data dependencies, data flow
modularity, abstraction, interface, encapsulation, divide
define communication protocol
types in a language
2
calculation - deterministic, we know what values it is gonna produce, can rely on it, stateless, declarative
side effect/action - non deterministic, can get/give different values when running the same effect, order of evaluation is important
effect - describes action result, but it is not run
3
concurrent code management - with continuations / pause and resume functions
continuation - capture the rest of computation at a given point - call stack, current instruction pointer
call stack
fiber - virtual stack frame, small heap-allocated, dynamically resized stacks. kept on the heap in memory, can be executed however and whenever
      - fibers represent the smallest undivideable atomic unit of concurrency in the runtime system.
      - between fibers we can interleave safely
      - in react - running effect/element/component
- pause work and come back to it later.
- assign priority to different types of work.
- reuse previously completed work.
- abort work if it's no longer needed.
4
error handling
5
functional core, imperative shell, onion architecture, domain logic in core
6
making time explicit
7
tracking granular changes in data/state and reacting only when data/state changes
8
going back in history easily
9
multiuser
10
state, state transitions



...
when working with D3
that needs to be in useEffect
imperatively creating updating deleting..
better to use react itself for it,
not to go back to imperative transition style in react itself
how to build custom renderer for that?

- let me just selectively rerender only parts of the tree to make it move - to make animations directly with the tree

- let me turn it into effect system,
integrate Effect Typescript

...
more then renderers:
https://github.com/enaqx/awesome-react?tab=readme-ov-file
...
react dom <div/>
react native 
react hardware <diode/>
react sequencer <track/> 
react three fiber <mesh/>
react pixi
react pdf
remotion
ink
react figma
