-------------------------------------------------
4
VIZ
NOT A CLOSED SYSTEM - USER EVENT - essence of interactivity
- x batch sequential program, where control is top to bottom 
    and in the hands of a developer
    - need to react to user events - callbacks, promises
    
- updating data
- browser catches event from the OS system
- updates DOM
- runs layout and render engine to rerender screen

...
- one main thread - deterministic order - where rendering happens, reacting to user events, js happens, dom - main orchestrator
- other threads - networking, encoding&decoding, crypto, monitoring input devices

- types of queued tasks - user interaction from OS, fetched data from network, worker data, 

- looping idle, processing task, requestAnimationFrame (running code that has anything to do with rendering) before render steps (style calculation, layout, painting)
    setTimeout with 0 - queues a task

- need to know order of instructions happening - so when our code depends on order, it is processed correctly, it is deterministic and we can control it
- in the big picture view, afaic, browser event loop is not deterministic - tasks can happen in any order - so we need to make code robust to order, not depend on it

(game loop is deterministic, event loop, even with rendering 60fps is not deterministic)
-------------------------------------------------
4 SCHEMA
NOT A CLOSED SYSTEM - USER EVENT
user inputs something and wants to see change
wants to interact
-   os receives that interaction from input device, passes it to browser process it - queues a task - places it into its task queue, 
    event loop picks it up, puts it on the dom, renders it to screen
-- how can we run our code in response to it?

--?? event loop, queues and threads

----------------------------
essence of an app - interactivity, user interaction, not a closed system
input
user typing into input
os sending event to browser
task queue
deterministic event loop on main thread
browser updates dom and displays what the user types
60 frps

long time is 200 ms on main thread

______________________________
references:
Jake Archibald on the web browser event loop, setTimeout, micro tasks, requestAnimationFrame, ...
https://www.youtube.com/watch?v=cCOL7MC4Pl0&t=1234s

https://github.com/atotic/event-loop

Reading 20: Callbacks and Graphical User Interfaces
https://web.mit.edu/6.031/www/sp22/classes/20-callbacks-guis/

Game Programming Patterns book
Game Loop and Event Queue chapter
game loop = deterministic order, ensuring a pass every 60fps

RF21 – Paul Henschel – Breaking out of the confines: making games in React
Render loop vs event-driven
https://www.youtube.com/watch?v=CLTZrFmri2E

Nat Goodspeed: The Fiber Library
https://www.youtube.com/watch?v=gcNphOWuUb0



------------------------------------------
main loop / game loop / animation loop / event loop

1.
update state
draw


2.
- deterministic state update with variable fps draw (depending on hardware)
constant update step
draw

3.
- interpolate draw calls
