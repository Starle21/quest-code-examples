aka UIHP - react UI hard parts - first atomic principles
----
data
declarative components in the form of functions - from 11
dataToDOM
convert - from 11
replaceChildren - from 11
----
all elements turned into functional components
createVDOM - from 12
render fce replaces dataToDOM fce
passing arguments/props as parameters to component functions
----
-   HOOKS - defined topmost App component, which 'owns' the data, render only on change, not handling hooks in multiple components
-   MULTIPLE USESTATE HOOKS IN ONE COMPONENT
----
DIFF ALGORITHM
mount and rerender phase
passing topmost component into render
----
MOUNT PHASE
HANDLING CREATE
- better createVDOM - calling functions inside, gathering effects into vDOM
- better convert - taking into account various types of elements
- passing root into render
RERENDER PHASE
HANDLING UPDATE
- BETTER DIFF AND CONVERT
HANDLING DELETE
dividing calculations and actions
- so update does not perform side effects
done:
- calculation and effect phase
- tagging effects in the calculation phase in reconciliation
- removing old event listeners on update
- creates root effect
- diff the same path for mount and rerender
- commit effects same fce for mount and rerender
- works with nested functional components
- topmost component can have hooks
-----
MEMOIZING components, MULTIPLE USESTATE HOOKS IN ALL COMPONENTS
-   memoization of functions, jsx - incrementalism
-   more components that need to store hooks
-   hooks - support for running function instead of just setting plain value
-   fiber, linked list, storage place for multiple components
-   before doing any calculation and diff(reconciliation), see if dependencies changed
-   dealing with different types of effects
-   jsx-like unwrapping fce
-----
when react rerenders, how can we manage that
commit
- runtime order of updates
- various hooks, order of hooks
- how to control triggers
-----
renderers
- various host implementations
-----
custom components, for boxing in imperative code
- defining non DOM element
-----
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
-----
outside state, inside state, caches
-----
escape hatches
-----
fiber async tasks
-   pause and resume in the middle of render calculation, virtual stack frame
-   control flow mechanism, easily switching between different points of execution
-   async stuff, suspense
----
dataflow system - ordered by data dependencies
live - yeet value back up the tree (effect system), continuations, quoting
effect system - Effect typescript
-----





...
NOW
element jsx
run jsx()
effect {...}
effect.function()
recurse effect.children

diff children
if new && old - update (tag, copy accessor pointer) / delete (array) - create (accessor)
if new && !old - create (accessor)
if !new && old - delete (array)

recurse commit
do the actions

...
NEXT
Root
create effect { ... }
effect.children = passed in jsx

Loop (effect)
diff if its effect exists (oldEffect && newEffect) 
diff - isCurrent
diff - same pointer props (root = null) (effect was not rerun above)
diff - isUpdateScheduled

based on type of effect (root, function, host)
root - reconcileChildren
functional - effect.function(), reconcile children
host - reconcileChildren

reconcileChildren
reconcileChildren - existsChildrenCurrentEffect
reconcileChildren - is type the same? 'div' or Component fce
reconcileChildren - return the same effect, or run jsx() and create new effect

- create new effect
element jsx
run jsx()
effect { ... }
return effect --> loop

...
fiber:
Once you have each stack frame as an object on the heap you can do clever things like reusing it during future updates and yielding to the event loop without losing any of your currently in progress data.

fiber tree = stack frames for running react component tree,
___
REFERENCES
https://pomb.us/build-your-own-react/
https://frontendmasters.com/courses/hard-parts-ui-dev/

__
https://www.youtube.com/watch?v=7GcrT0SBSnI