-------------------------------------------------
REACT STRATEGIES

4. react strategies
- dataflow
- view = f(state)
- component = f(props * state)
- new state = f(previous state, event)


____
data transformation pipeline view (functional approach)
divide between (sandwich:)
- state (input) at the top / near the top (of a tree or subtree)
- calculation
- effect

- trigger - gathering all inputs
- calculation of new dependencies tree
- updating dom - visual change on screen

____
VIZ
calculation = dataflow graph view
- ordered by data dependencies (execution flow follows data dependencies)
dependency tree, 2 types
  - ui components hierarchical tree (what is nested in what), and its ui state
    - view = f(state)
    - component = f(props * state)
        - state changed, props the same - self/child changed
        - state the same, props changed - parent state changed
        - props changed, state changed - parent's props/state changed triggered child's state change
    - dependecies - props, placement in parent widget
    - trigger of rerender is change in state of a parent component
        - then all child components get rerendered

  - domain data dependencies, can be shown in multiple places
    - recoil
    - new state = f(previous state, event)

____
11 GRAPHICS
dataflow graph view
trigger, inputs --> state of first node --> props, state of child node
?


no code (higher level "blocks" at most), just graphics