____
consistency between copies of data and derived values
cycles in connections (event graphs complected)
- when there are multiple places where data can be changed
- no single place for source of particular data


our data global - make it modular - each context will keep its own data, code owns its data --> but synchronization issues
now we need some type of communication between modules - eg calling listeners with data - callbacks to pass data around - when event happens or when data change happens
- synchronous communication through observables (implemented)
    - ?? creates race condition? - when network data arrives, while user is inputing - probably not, I did not define some function in the state machine..
- asynchronous using task queue (not implemented)
- using streams ??[need to research better] - FRP and other types
issues:
two modules in our system in which we ingest/write the same data - from user and from network
3 places where data 'lives' - in DOM for inputs, in Svg module, in Store module
side effects are performed, when executing defined handlers after event happens (dispatchEvent executes the listeners synchronously)

______
consistency between copies hard to maintain
2 places where changed data can be written into


---
into DOM input
fetch from network - add network request

user inputs new number into input - value in dom node input changes, handler is scheduled - updates "view"
new data comes - handler is scheduled - updates input xCoord in JS memory - updates "model"
ending up with different states - sometimes dom input wins, sometimes network request

...
await fetch
write to network data
render to screen - call accessor setter

...
user event - dom and screen gets updated automatically
call handler
write to view data - call accessor get

...
what is the order of tasks run by event loop?
can some bad interleaving of tasks create inconsistency?

