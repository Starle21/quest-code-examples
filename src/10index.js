// full react version, using original react

import ReactDOM from "react-dom/client";
import React from "react";
import { App } from "./ui";

const root = ReactDOM.createRoot(document.getElementById("app"));
root.render(<App />);
